---
import BaseLayout from '../../layouts/BaseLayout.astro'
import Section from '../../components/Section.astro'
import { getPageMeta } from '../../seo'
import { getTranslations } from '../../i18n/utils'

const pageMeta = getPageMeta('en', 'privacy')
const dict = getTranslations('en') as any
const pp = dict.privacy_page
const sections = pp.content.sections as Array<{ title: string; text: string }>
---

<BaseLayout
  title={pageMeta?.title}
  description={pageMeta?.description}
  esUrl="https://vunotek.com/privacidad/"
  enUrl="https://vunotek.com/en/privacy/"
>
  <Section class="!pt-32 md:!pt-40 !pb-xl md:!pb-2xl">
    <div class="max-w-[48rem] mx-auto px-margin">
      <div class="flex flex-col gap-xs mb-xl">
        <span class="font-label-mono text-label-mono text-electric-blue">// privacy_policy.md</span>
        <h1 class="font-display-lg text-headline-lg text-on-surface">{pp.content.title}</h1>
        <span class="font-label-mono text-[11px] text-slate-text/50">{pp.content.last_updated}</span>
      </div>
      <div class="space-y-xl">
        {sections.map((section) => (
          <div class="space-y-sm">
            <h2 class="font-headline-sm text-headline-sm text-electric-blue">{section.title}</h2>
            <p class="font-body-md text-body-md text-slate-text leading-relaxed">{section.text}</p>
          </div>
        ))}
      </div>
    </div>
  </Section>
</BaseLayout>
